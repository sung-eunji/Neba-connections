import * as magic_string from 'magic-string';

declare function reactComponentTagger({ exclude, extensions, }?: {
    exclude?: string[];
    extensions?: string[];
}): {
    name: string;
    enforce: string;
    transform(code: string, id: string): Promise<{
        code: string;
        map: magic_string.SourceMap;
    } | null | undefined>;
};

export { reactComponentTagger };
